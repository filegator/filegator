/// <reference types="node" />
/// <reference types="node" />
import { ScanClient } from './protos/scan_grpc_pb';
import { AmaasScanResultObject } from './amaasScanResultObject';
import { AmaasScanResultVerbose } from './amaasScanResultVerbose';
import { Logger } from './logger';
export declare class ScanRun {
    private readonly scanClient;
    private readonly deadline;
    private readonly logger;
    private finalResult;
    private readonly tags;
    private readonly bulk;
    constructor(scanClient: ScanClient, timeout: number, logger: Logger, tags?: string[]);
    private streamRun;
    private handleStreamData;
    scanFile(name: string, size: number, pml: boolean, feedback: boolean, verbose: boolean, digest: boolean): Promise<AmaasScanResultObject | AmaasScanResultVerbose>;
    scanBuffer(name: string, buff: Buffer, pml: boolean, feedback: boolean, verbose: boolean, digest: boolean): Promise<AmaasScanResultObject | AmaasScanResultVerbose>;
}
