/// <reference types="node" />
/// <reference types="node" />
import { AmaasScanResultObject } from './amaasScanResultObject';
import { AmaasScanResultVerbose } from './amaasScanResultVerbose';
import { AmaasCredentials } from './amaasCredentials';
import { LogLevel } from './logger';
type LogCallback = (level: LogLevel, message: string) => void;
/**
 * Class AmaasGrpcClient
 */
export declare class AmaasGrpcClient {
    private readonly scanClient;
    private readonly credentKey;
    private readonly timeout;
    private readonly logger;
    private readonly appNameHeader;
    /**
     * AmaasGrpcClient constructor
     * @param amaasHostName - AMaaS host name or region name
     * @param credent - AmaasCredentials object
     * @param timeout - number in seconds to wait before closing the connection
     * @param enableTLS - enabling TLS
     * @param caCert - full path name of CA certificate pem file for self hosted scanner server. null if using Trend scanner services.
     * @param appName - application name
     */
    constructor(amaasHostName: string, credent: AmaasCredentials | string, timeout?: number, enableTLS?: boolean, caCert?: null, appName?: string);
    /**
     * Process error
     *
     * @param err - Error to process
     */
    private processError;
    private initScanRun;
    /**
     * Scan file and return result
     *
     * @param name - Filename
     * @param tags - Tags to be added to the scan request
     * @param pml - Flag to enable predictive machine learning detection.
     * @param feedback - Flag to use Trend Micro Smart Protection Network's Smart Feedback.
     * @param verbose - Flag to enable verbose mode in returning scan result.
     * @param digest - Flag to enable calculation of digests for cache search and result lookup.
     */
    scanFile(name: string, tags?: string[], pml?: boolean, feedback?: boolean, verbose?: boolean, digest?: boolean): Promise<AmaasScanResultObject | AmaasScanResultVerbose>;
    /**
     * Scan buffer and return scan result
     *
     * @param fileName - Filename
     * @param buff - Buffer to scan
     * @param tags - Tags to be added to the scan request
     * @param pml - Flag to enable predictive machine learning detection.
     * @param feedback - Flag to use Trend Micro Smart Protection Network's Smart Feedback.
     * @param verbose - Flag to enable verbose mode in returning scan result.
     * @param digest - Flag to enable calculation of digests for cache search and result lookup.
     */
    scanBuffer(fileName: string, buff: Buffer, tags?: string[], pml?: boolean, feedback?: boolean, verbose?: boolean, digest?: boolean): Promise<AmaasScanResultObject | AmaasScanResultVerbose>;
    /**
     * Close scan client
     */
    close: () => void;
    setLoggingLevel: (level: LogLevel) => void;
    configLoggingCallback: (cb: LogCallback) => void;
}
export {};
